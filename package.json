{
  "name": "industree-static-archive",
  "private": true,
  "scripts": {
    "test": "npm run test:syntax && npm run test:smoke",
    "test:smoke": "playwright test",
    "test:syntax": "node --check site/assets/index.js && node --check site/assets/impulse-player.js && node --check playwright.config.js && node --check tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
